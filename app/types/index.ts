export interface ServiceType {
  icon: React.ReactNode;
  title: string;
  description: string;
}

export interface ToolType {
  name: string;
  percentage: string;
}

export interface StatType {
  number: string;
  text: string;
}